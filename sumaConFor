#include<stdio.h>
int main(){
    int numero, i, suma=0;
    printf("Que numero vamos a usar");
    scanf("%d", &numero);
    for ( i = 1; i <=numero; i++){
        printf("%d + ", i);
        suma=suma+i;
    }
    printf("= %d", suma);
}
